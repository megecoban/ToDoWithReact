
import { Icon } from '@chakra-ui/react'
import {SettingsIcon, PersonIcon} from '@mui/icons-material/Settings';

export function GetSettingsIcon() {
    return <Icon as={SettingsIcon} />
  }

export function GetPersonIcon() {
      return <Icon as={PersonIcon} />
    }